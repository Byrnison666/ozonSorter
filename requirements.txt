# OzonSorter — стек под Windows 8.1 (целевая платформа).
# Python 3.10 — последний, под который есть колёса PySide2 5.15.2.1
# (на 8.1 формально работает до 3.12, но PySide2 кончается на 3.10).
# PySide2 (Qt5) — Qt6/PySide6 требует Windows 10, на 8.1 не запускается.
PySide2==5.15.2.1
SQLAlchemy>=2.0,<3
openpyxl>=3.1
