# OzonSorter — стек под Windows 8.0 (целевая платформа).
# Python 3.8 — последний, поддерживающий Windows 8.0 (3.9+ требуют 8.1).
# PySide2 (Qt5) — Qt6/PySide6 не запускается на Win8.
PySide2==5.15.2.1
SQLAlchemy>=2.0,<3
openpyxl>=3.1
