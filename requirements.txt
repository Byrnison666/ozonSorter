# OzonSorter — runtime deps. Точные версии PyInstaller вырезал из бандла;
# здесь нижние границы, совместимые с Python 3.14 (рантайм оригинальной сборки).
# Зафиксируй точные версии через `pip freeze` после первой удачной сборки.
PySide6>=6.8
SQLAlchemy>=2.0
openpyxl>=3.1
